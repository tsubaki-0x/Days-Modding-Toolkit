# Formato ORS

Status: integração futura.

Conhecimento observado durante o patch PT-BR:

- comandos como `[PrintText]`, `[PlayVoice]`, `[CreateBG]` e `[SetSELECT]`;
- separação sensível a TAB;
- término de comandos com `;`;
- scripts encontrados em `Script/english/00` até `05`.

O leitor e o writer usados anteriormente devem ser revisados e testados antes
de serem incorporados como API estável do toolkit.

